version: '3.2'

services:
   fakesmtp:
     image: giorgiocasciaro/civil-microservices-fakesmtp:0
     ports:
       - 25:25
     networks:
       - microservicesNet

networks:
  microservicesNet:
    driver:             bridge
