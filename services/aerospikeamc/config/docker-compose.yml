version: '3.2'

services:
   aerospikeamc:
    #  logging:
    #    driver: none
     image: giorgiocasciaro/civil-microservices-aerospike-amc:0
     networks:
       - microservicesNet
       - aerospike

networks:
  microservicesNet:
    driver:             bridge
  aerospike:
    driver:             bridge
